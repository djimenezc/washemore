export default (state = {}, action) => {

  switch (action.type) {

    // case actionTypes.FETCH_USERS_PENDING : {
    //   state = {...state, fetching: true};
    //   break;
    // }

  }

  return state;
};
